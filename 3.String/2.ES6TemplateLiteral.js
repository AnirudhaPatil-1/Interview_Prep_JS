//template literal
// 1. variable in string
let name = "Anirudha"
let str = `Hello my name is ${name}`;
console.log(str);

// 2. store the given format
str = `this stores
            the format  
                as it is saved inside 
                        backticks`;
console.log(str);
/*output -> 
this stores
            the format  
                as it is saved inside 
                        backticks
*/                        